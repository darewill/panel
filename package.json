{
  "scripts": {
    "build": "esbuild src/App.jsx --bundle --outdir=pub --servedir=pub --minify --watch"
  },
  "dependencies": {
    "esbuild": "0.18.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.2"
  }
}
